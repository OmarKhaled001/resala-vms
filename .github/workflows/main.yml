name: Deploy to Server
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Server Update
        run: |
          curl -X POST http://162.0.232.113:5000/webhook -H "Content-Type: application/json" -d '{"ref":"refs/heads/main"}'
